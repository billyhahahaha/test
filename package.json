{
  "name": "angeles-national-green-guide",
  "version": "1.0.0",
  "private": true,
  "description": "iOS-style green guide web app for Angeles National Golf Club",
  "scripts": {
    "build": "echo \"static site — no build step required\"",
    "start": "sirv public --no-clear --single",
    "dev": "sirv public --no-clear --single --dev"
  },
  "dependencies": {
    "sirv-cli": "^2.0.0"
  }
}
